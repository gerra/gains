<?xml version="1.0" encoding="utf-8"?>
<!-- The languages the app speaks, so Android 13+ lists it under per-app language settings. Keep in
     step with app.gains.i18n.Strings.all and the values-* folders. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="ru" />
</locale-config>
