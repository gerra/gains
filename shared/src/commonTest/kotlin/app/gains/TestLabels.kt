package app.gains

import app.gains.analysis.UnitLabels

/** The English unit words, as the UI's resources would supply them. */
val englishLabels = UnitLabels(kg = "kg", lbs = "lbs", second = "s", minute = "min", hour = "h", km = "km", reps = "reps")
